const CardContainer = ({ title }) => {
    return (
        <div class="card">
            <div class="container">
                <h1>{ title }</h1>
            </div>
        </div>
    )
}

export default CardContainer
